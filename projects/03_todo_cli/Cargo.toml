[package]
name = "todo_cli"
version = "0.1.0"
edition = "2021"
description = "Todo CLI - 命令行待办事项管理器"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
